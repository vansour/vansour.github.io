# vansour blog 项目级强约束

本文件是本仓库的操作规约。处理任何任务前必须先阅读本文件，并在任务结束前逐项执行适用的检查。

本文件不能覆盖系统指令、开发者指令或用户在当前对话中的最新明确要求；发生冲突时按更高优先级执行，并说明冲突。

## 1. 不可违反的规则

- 不得把未读过的代码、配置或构建结果当作已确认事实。
- 不得为了完成任务执行 `git reset --hard`、`git checkout --`、大范围 `rm -rf` 或其他不可恢复操作。
- 不得覆盖、回滚或删除用户已有的未提交改动；发现相关改动时先阅读并在其基础上工作。
- 不得读取、输出或提交密钥、令牌、密码、私钥、`.env` 内容或其他敏感信息。
- 不得擅自扩大任务范围，不得因为“顺手优化”修改无关文件、依赖或文案。
- 不得在未获明确授权时提交、推送、创建远程资源、发布版本或改变部署设置。
- 不得在未获明确授权时新增运行时框架、测试框架、构建集成或大型依赖。
- 不得主动委派给其他代理；只有用户或更高优先级指令明确要求时才可委派。
- 不得伪造测试结果、审查结论、远程状态或“已修复”声明；未验证的内容必须标为推测或未验证。

## 2. 仓库事实与技术契约

- 项目是 `vansour` 的中文个人技术博客，默认分支为 `main`。
- 远程仓库：`git@github.com:vansour/vansour.github.io.git`。
- 部署目标是 GitHub Pages 根路径和自定义域名 `https://mhy.im`；不要擅自添加 `base` 路径。
- 使用 Astro 静态输出、Content Collections、原生 JavaScript、CSS；不使用 React/Vue/Svelte 等 UI 框架。
- Node.js 必须使用 26（以 `.nvmrc` 和 `package.json.engines` 为准）。
- 包管理器是 npm；依赖变更必须同时审阅 `package.json` 和 `package-lock.json`。
- 依赖声明遵循“最新稳定版本”政策；锁文件负责可复现构建，不能用无关任务中的随意升级破坏它。
- 当前构建链是：清理 `.astro` → `astro build` → `pagefind --site dist`。
- `astro.config.mjs` 使用 `build.format: 'file'`；构建产物中的页面是 `.html` 文件。
- `public/CNAME`、`public/robots.txt`、MiSans 字体和图标属于发布契约，不得随意移除或改名。

## 3. URL、SEO 与发布契约

- 文章永久链接格式为 `/blog/<slug>.html`，其中 slug 来自 Markdown 文件名。
- RSS、卡片、全部文章页和搜索结果必须指向同一套文章 URL；改 slug 必须考虑旧链接和 RSS 兼容。
- 顶层页面、分页页面、canonical、RSS、sitemap 的 URL 必须在生产构建后检查，不能只看开发服务器。
- 首页 canonical 应代表根 URL；不能无意中制造 `/` 与 `/index.html` 的重复 canonical。
- sitemap 必须继续排除 404 页面，并保持自定义域名地址。
- Pagefind 索引只应覆盖实际可搜索的页面正文；导航、页脚、错误提示和临时内容不得成为搜索结果。
- 站点仍是根路径部署；不要用相对路径替代现有绝对根路径，除非同时验证 GitHub Pages 和本地预览。

## 4. 内容模型与写作规则

文章位于 `src/content/blog/`，frontmatter 契约定义在 `src/content.config.ts`：

- `title: string` 必填。
- `description: string` 必填，用于列表和 SEO。
- `order: number` 可选，数值越小越靠前；未设置的文章排在最后。
- `draft: boolean` 可选，默认为 `false`；开发环境可见，生产构建必须剔除。

除非用户明确要求，不要重新引入日期、标签、归档、评论、统计、后台、MDX 或多语言系统。

新增或编辑文章时必须：

1. 保持标题、摘要、文件名和链接语义一致。
2. 对破坏性命令、root 权限、远程脚本、清盘、删除数据等行为保留清晰警告。
3. 不擅自改变命令的参数、版本、来源或执行顺序；需要更新外部命令时先核对官方来源或用户提供的上下文。
4. 文章中的代码块必须在生产构建产物中检查换行、转义、滚动和复制文本。

## 5. `code-tabs` 专用约束

- `src/plugins/code-tabs.ts` 是构建期 HAST 插件，语法是 `code-tabs <lang>` 围栏、维度头部、`---` 分隔线和模板占位符。
- 组合数上限是 24；新增维度前必须计算笛卡尔积，超过上限必须让构建失败而不是静默截断。
- 当前明确关闭 Astro 语法高亮（`syntaxHighlight: false`），代码变体是纯文本 DOM；不得假定一定存在 Shiki 的 `.line` 节点。
- 修改 `CodeTabs.astro` 或插件时，必须验证下拉维度、输入维度、复制按钮、无 JavaScript 降级和长代码横向滚动。
- 混合输入维度与下拉维度属于独立场景，不能只用当前文章样例证明它可用。
- 用户输入替换必须使用文本节点或安全的 DOM API；不得把输入值拼接成可执行 HTML。
- 复制文本必须来自当前可见变体，并保留原始换行。

## 6. 前端与视觉约束

- 优先复用现有组件、CSS 变量、断点和命名；不要为一个局部需求引入新的设计系统。
- 颜色必须优先使用 `global.css` 的设计令牌；主题和强调色要在亮色、暗色及所有现有强调色下工作。
- 保持 MiSans 字体、中文文案、品牌名 `vansour blog` 的既有大小写风格，除非用户明确要求变更。
- 保持桌面、平板、640px 以下手机和 480px 以下窄屏导航的可用性；不得让文字、按钮、代码或分页互相遮挡。
- 交互控件必须有语义元素、可见焦点、合适的 `aria-*` 状态和键盘操作；不要只依赖 hover。
- 不得把页面主体改成营销式落地页，也不要添加与技术博客无关的装饰、动画或图片。
- 页面没有客户端路由框架；脚本必须在完整页面加载和无脚本降级下都不破坏正文。
- 任何视觉改动都要同时检查文章页、首页、关于页、404 页和移动断点，而不是只检查单一页面。

## 7. 组件职责边界

- `BaseLayout.astro`：HTML 骨架、公共 head、主题首帧初始化和公共组件挂载。
- `Header.astro`：品牌、导航、搜索入口、主题入口和移动导航。
- `SearchDialog.astro`：Pagefind 懒加载、快捷键、结果转义和弹窗状态。
- `ThemeToggle.astro`：`theme`/`accent` 的读取、写入和状态同步。
- `CopyCodeButton.astro`：普通代码块复制，不得重复处理 `.code-tabs` 内变体。
- `CodeTabs.astro`：多变体选择、输入替换和当前变体复制。
- `PostCard.astro`：列表卡片展示，不在其中实现文章查询或排序。
- `src/lib/posts.ts`：唯一的文章过滤和排序入口；列表、分页、详情、RSS 必须复用它。

若某项需求跨越上述边界，先说明接口变化，再修改；不要把业务逻辑散落到多个页面。

## 8. 依赖与工具规则

- 默认使用锁文件中已验证的依赖版本；不得因为“最新”而在无关任务中升级依赖。
- 依赖升级必须说明升级原因、兼容性影响、锁文件变化和回滚方案，并运行完整生产构建。
- `@astrojs/markdown-satteri` 当前由 Astro 依赖链提供；修改其导入或升级 Astro 前必须确认 API 仍兼容。
- 不要为了执行一次检查临时把工具写入 `package.json`；确有必要时先说明并获得授权。
- 优先使用 `rg`/`rg --files` 搜索，优先并行执行互不依赖的只读检查。
- 手工编辑必须使用 `apply_patch`；不得用 shell 重定向、`cat >` 或 Python 脚本写入项目文件。

## 9. 每次任务的强制流程

### 开始前

1. 阅读本文件及任务涉及的源码、配置和文档。
2. 运行 `git status --short --branch`，记录已有改动，不触碰无关文件。
3. 用 `rg` 搜索相关调用点、路由、样式和文档引用。
4. 明确假设、影响范围和验证方式；涉及破坏性命令、外部服务或身份权限时先停下来请求确认。

### 编辑时

1. 先在 commentary 中说明即将修改的文件和行为。
2. 保持改动最小、可审阅、可回滚；不要顺手格式化整个仓库。
3. 新增抽象必须能减少真实复杂度，且符合现有命名和目录边界。
4. 同步更新受影响的 README、设计文档或注释，避免留下与实现冲突的说明。

### 完成前

1. 涉及 `.astro`、`.ts` 或客户端脚本时，运行 `npm run check`；任何诊断都必须处理或在交付中明确说明。
2. 运行与改动风险匹配的验证；涉及页面、内容、路由或构建时插件时至少运行 `npm run build`。
3. 检查 `dist` 中的实际 HTML、链接、canonical、RSS、sitemap 和 Pagefind 结果，而不只看终端“构建成功”。
4. 运行 `git diff --check`，审阅 `git diff --stat` 和完整差异。
5. 再次确认没有修改用户原有改动、没有生成敏感文件、没有留下运行中的开发服务器或交互式进程。

## 10. Git 与提交规则

- 默认只编辑工作区，不自动 commit、push、开 PR 或改远程设置。
- 只有用户明确要求提交时才创建 commit；只有用户明确要求推送时才 push 到 `origin/main`。
- 提交作者必须是 `vansour` 本人；提交前检查 Git 身份，不得冒用或猜测其他身份。
- 提交信息必须简洁、描述实际变更，不得包含任何代理工具、生成器、协作者尾注或宣传性署名。
- 不得改写公共历史，不得强推，不得删除远程分支。
- 提交前必须确认 diff 只包含本任务内容，并明确告知 commit id、验证结果和未解决风险。

## 11. 交付报告格式

默认使用中文，先给结论，再给必要细节。每次完成后必须说明：

- 修改了哪些文件，以及每个文件的行为变化。
- 运行了哪些命令、结果是什么；失败或未运行的检查必须明确写出原因。
- 是否存在已知限制、兼容性风险、需要用户决定的事项。
- 若没有修改代码，也要说明实际阅读和验证范围，不能用泛泛的“已处理”代替证据。

不得以“应该可以”“看起来没问题”作为唯一结论；结论必须对应代码、构建产物或命令输出。
