# CLAUDE.md

vansour 的个人博客仓库（Astro + GitHub Pages）。

## Git 远程仓库

- 远程仓库地址：`git@github.com:vansour/vansour.git`
- 默认分支：`main`
- 所有提交推送到该远程仓库。

## 提交规范

- 提交到 GitHub 时，提交作者（author）仅允许为 `vansour` 本人。
- 提交信息及任何提交内容中不得出现与 Claude Code、Claude、Anthropic 相关的任何内容（包括但不限于 `Co-Authored-By: Claude` 等署名尾注、`🤖 Generated with Claude Code` 等说明文字）。

## 开发环境

- Node.js 版本默认使用 26（通过 `.nvmrc` / package.json 的 `engines` 字段锁定）。

## 依赖版本

- 所有依赖库（包、函数、工具）一律使用最新稳定版本。
