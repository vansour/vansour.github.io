---
import type { CollectionEntry } from 'astro:content';

const { post } = Astro.props;
const { data, id } = post;
---

<article class="post-card">
  <h2>
    <a href={`/blog/${id}.html`}>{data.title}</a>
  </h2>
  <p class="post-excerpt">{data.description}</p>
</article>
